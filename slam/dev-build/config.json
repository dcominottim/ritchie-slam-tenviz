{
  "dockerImageBuilder": "ritclizup/rit-shell-bat-builder",
  "inputs": [
    {
      "cache": {
        "active": true,
        "newLabel": "Type new value. ",
        "qty": 3
      },
      "default": ".",
      "label": "Type the build context root dir",
      "name": "rit_root_dir",
      "required": true,
      "tutorial": "The directory that will be used as build context for the 'docker build' command",
      "type": "text"
    }
  ],
  "template": "shell-bat",
  "templateRelease:": "2.16.2"
}
